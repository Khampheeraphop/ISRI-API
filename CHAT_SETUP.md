# ผู้ช่วยแชท ISRI

แชทใหม่แยกจาก AI ประเมินเหตุเดิม ใช้ Edge Function `isri-chat` และ Gemini สำหรับตีความคำถามและเรียบเรียงคำตอบจากข้อมูลที่เซิร์ฟเวอร์อนุญาตเท่านั้น ไม่แก้สถานะงาน ไม่แลกรางวัล และไม่เปิดให้โมเดลเขียน SQL

## ขอบเขต

| บทบาท     | ข้อมูลที่รองรับ                                                                          |
| --------- | ---------------------------------------------------------------------------------------- |
| ผู้แจ้ง   | รายการของตน แต้มที่ใช้ได้ แค็ตตาล็อกรางวัล ส่วนต่างแต้ม และประวัติการแลกของตน            |
| ผู้จัดสรร | คิวรอจัดสรรและงานที่ตนจัดสรร                                                             |
| ช่าง      | งานที่ยังมีชื่ออยู่ในทีม และ PM ที่ได้รับมอบหมาย                                         |
| ผู้ดูแล   | ภาพรวมรายการแจ้งเหตุ PM สต็อกรางวัล การส่งมอบ ข้อมูลแคมเปญ และจำนวนผู้ใช้ตามสถานะอนุมัติ |

ผู้ใช้ถามต่อเนื่องได้ ประวัติเก็บในหน่วยความจำหน้าเว็บเท่านั้น เปลี่ยนบัญชี/บทบาท โหลดหน้าใหม่ หรือออกจากระบบจะล้างบทสนทนา คำขอส่งสามคู่สนทนาล่าสุดและคำถามใหม่ โมเดลต้องอ่านข้อมูลใหม่ทุกครั้งที่ตอบข้อเท็จจริง

รายการแบ่งหน้าละ 15 รายการพร้อมยอดรวมจาก `count: exact` และยอดรวมตามสถานะ ช่วงวันที่เป็นวันตามเวลาไทย รวมวันสุดท้าย โดยกรองวันที่สร้างเหตุ/มอบหมายงาน/ครบกำหนด PM/เริ่มแคมเปญตามประเภทข้อมูล ประวัติซ่อม 10 เหตุการณ์ล่าสุดจะถูกอ่านเมื่อค้นพบรายการเดียวด้วยรหัส ไม่ได้รองรับการค้นเอกสาร อันดับแคมเปญ หรือวิเคราะห์ประวัติการปฏิบัติงานทุกประเภท

## ตั้งค่า

ตั้ง secrets ฝั่ง Supabase Edge Functions:

```env
GEMINI_API_KEY=<API key ของโครงการ Gemini>
GEMINI_CHAT_MODEL=gemini-3.6-flash
ISRI_CHAT_ENABLED=true
```

Local: คัดลอก `supabase/functions/.env.example` เป็น `supabase/functions/.env` หากยังไม่มีไฟล์ หากมีอยู่แล้วให้เพิ่มเฉพาะค่าของแชท อย่าเขียนทับ secrets เดิม แล้วรัน Supabase ตามคู่มือ API

Cloud: ตั้งค่าใน Supabase Dashboard > Edge Functions > Secrets แล้ว deploy:

```powershell
npx supabase functions deploy isri-chat --project-ref <project-ref>
```

คง JWT verification ไว้ ฟังก์ชันตรวจ user JWT, บัญชี approved และบทบาทจากฐานข้อมูลซ้ำ ก่อนเข้าถึงข้อมูล ห้ามใส่ API key ในตัวแปร `VITE_*`, หน้าเว็บ หรือแชท ไม่ใช้ `GEMINI_MODEL` ของ AI เก่าโดยอัตโนมัติ

หน้าต่างแชทเรียก `GET /functions/v1/isri-chat` เพื่อตรวจว่าตั้งค่าเปิดใช้แล้วหรือไม่ การตอบจริงใช้ `POST` พร้อม `{ messages: [{ role: "user", text: "ฉันมีแต้มเท่าไหร่" }] }` และ JWT ของผู้ใช้ ไม่มีพารามิเตอร์ userId หรือ role จากหน้าเว็บ

หลังตั้งค่าแล้วต้องทดสอบด้วยบัญชีทดสอบของทั้ง 4 บทบาทบนฐานข้อมูลเป้าหมาย การมี secrets ครบไม่ได้ยืนยันว่า key หรือ model พร้อมจริง หากฟังก์ชัน/โมเดลไม่พร้อม หน้าแชทจะแสดงข้อผิดพลาด ไม่แสดงข้อมูลจำลองแทนคำตอบ

## การควบคุม

- ระบบ ISRI ไม่จำกัดจำนวนครั้งในการแชท หากโควตาของ Gemini หมด ฟังก์ชันจะแจ้ง `GEMINI_QUOTA` เพื่อให้ผู้ดูแลตรวจสอบหรือเปลี่ยน `GEMINI_API_KEY` ใน Supabase
- สูงสุด 2 คำขอต่อโมเดลต่อหนึ่งคำถาม และอ่านไม่เกิน 2 หัวข้อ ไม่ใช้เว็บค้นหาหรือเครื่องมือแก้ไขข้อมูล
- จำกัดข้อความใหม่ 1,500 ตัวอักษร และ payload 100 KB มี timeout สำหรับผู้ให้บริการ
- ปิดฟีเจอร์ด้วย `ISRI_CHAT_ENABLED=false` ได้โดยไม่กระทบระบบงานหลัก
- ไม่บันทึกเนื้อหาแชทหรือ provider response ลง log
- ส่งเฉพาะฟิลด์ที่เลือก ไม่ส่งชื่อ อีเมล โทรศัพท์ หรือที่อยู่จัดส่งจากตารางบัญชี/การแลกรางวัล มีการปิดบังรูปแบบอีเมลและโทรศัพท์ในข้อความอิสระเบื้องต้น ซึ่งไม่ครอบคลุมข้อมูลอ่อนไหวทุกชนิด จึงไม่ควรใส่ข้อมูลผู้ป่วยในรายละเอียดหรือแชท ต้องตั้งค่าการใช้ข้อมูลฝั่งผู้ให้บริการให้เหมาะสมก่อนใช้ข้อมูลจริง
- โมเดลคัดกรองขอบเขตทั้งก่อนอ่านข้อมูลและก่อนตอบ แต่ไม่ได้รับประกันความถูกต้องของภาษาที่สร้างขึ้น 100% สิทธิ์ข้อมูลบังคับด้วยโค้ด และลิงก์อ้างอิงสร้างจากรายการที่ผ่านสิทธิ์เท่านั้น

## ตรวจสอบ

```powershell
.\.tools\deno.exe task check
.\.tools\deno.exe task test
```

ฝั่งเว็บใช้ `npm run build` และ `npm run lint` การทดสอบอัตโนมัติจำลองผู้ให้บริการ AI และข้อมูลเพื่อทดสอบสิทธิ์/error paths; ต้องทดสอบความเข้าใจภาษาไทยและคำตอบกับโมเดลจริงหลังตั้ง secrets เพิ่มด้วย เช่น ถามแต้ม ถามรายการของคนอื่น สวมบทบาท admin ถามนอกระบบ ถามต่อด้วย “รายการที่สอง” และจำลองโมเดลล่ม

อ้างอิงการเชื่อมต่อ: [Gemini generateContent](https://ai.google.dev/api/generate-content), [Structured outputs](https://ai.google.dev/gemini-api/docs/structured-output), [รายชื่อโมเดล](https://ai.google.dev/gemini-api/docs/models)
