# ISRI Email Delivery

ระบบอีเมลของ ISRI ใช้ **Resend** ผ่าน Edge Function เท่านั้น โดยไม่มีคีย์ใดถูกส่งไปที่ frontend
และไม่มีโลโก้ภายนอกในอีเมล: ใช้คำว่า **ISRI** เป็น wordmark ตามหน้าตาที่อนุมัติไว้

## ตั้งค่า Secrets บน Supabase

เพิ่มค่าเหล่านี้ใน Project Settings > Edge Functions > Secrets:

```text
RESEND_API_KEY=re_...
EMAIL_FROM=ISRI <noreply@your-verified-domain.com>
APP_URL=https://isri.netlify.app
```

- `RESEND_API_KEY` ต้องเป็นคีย์สำหรับส่งอีเมลเท่านั้น และเก็บไว้ใน Supabase Secrets
- โดเมนใน `EMAIL_FROM` ต้องยืนยันกับ Resend แล้ว
- `APP_URL` คือ URL ของหน้าเว็บ production ที่อีเมลจะพาผู้ใช้กลับเข้าระบบ

หากยังไม่ตั้งค่าครบ ระบบจะบันทึกอีเมลไว้ใน `email_outbox` เป็น `pending` แต่จะไม่ให้การ
แจ้งเหตุ มอบหมายงาน หรืออนุมัติงานล้มเหลว

## เหตุการณ์ที่ส่งอีเมล

| เหตุการณ์                | ผู้รับ                                   |
| ------------------------ | ---------------------------------------- |
| ผู้แจ้งส่งคำขอ           | ผู้จัดสรรงานที่อนุมัติแล้วทุกคน          |
| ไม่รับรายการ             | ผู้แจ้งเหตุ                              |
| มอบหมายงาน               | ผู้แจ้งเหตุ, ช่างหลัก, ช่างสนับสนุนทุกคน |
| ช่างหลักรับงาน           | ผู้แจ้งเหตุ, ผู้จัดสรรงานที่มอบหมาย      |
| ขอเบิกอะไหล่             | ผู้จัดสรรงานที่มอบหมาย                   |
| อนุมัติ/ไม่อนุมัติอะไหล่ | ช่างหลัก                                 |
| ส่งผลการซ่อม             | ผู้จัดสรรงานที่มอบหมาย                   |
| ส่งกลับแก้ไข             | ช่างหลัก                                 |
| ตรวจรับและปิดงาน         | ผู้แจ้งเหตุ                              |

ช่างสนับสนุนได้รับอีเมลเฉพาะตอนถูกมอบหมายงาน เพื่อไม่ให้ถูกส่งอีเมลซ้ำจากการตัดสินใจที่ช่างหลักต้องดำเนินการ

## การส่งซ้ำด้วยผู้ดูแลระบบ

หลังตั้งค่า Secrets แล้ว ผู้ดูแลระบบสามารถเรียก endpoint นี้เพื่อประมวลผลคิวค้างได้:

```text
POST /admin/email-outbox/process
{ "limit": 25 }
```

คิวใช้ idempotency key ต่อรายการ และจะ retry อัตโนมัติแบบหน่วงเวลาเมื่อผู้ให้บริการตอบกลับผิดพลาด
สูงสุด 5 ครั้ง โดยการส่งอีเมลล้มเหลวจะไม่ย้อนสถานะของ workflow
